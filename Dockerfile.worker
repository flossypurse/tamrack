FROM node:20-slim

ARG GIT_SHA=
ENV GIT_SHA=$GIT_SHA

WORKDIR /app

# Install deps
COPY package.json package-lock.json ./
RUN npm ci --omit=dev && npm install tsx

# Copy source
COPY . .

CMD ["npx", "tsx", "worker.ts"]
